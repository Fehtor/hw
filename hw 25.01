#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
using namespace std;

int main() {
  string a, b, c, d;
  cin >> a >> b >> c;

  d = a + b;
  sort(d.begin(), d.end());
  sort(c.begin(), c.end());

  if(d.size() == c.size()){
    for(int i = 0; i < d.size(); i++){
      if(d[i] != c[i]){
          cout << "No";
          return 0;
        }
    }
  if(d.size() != c.size()) {
    cout << "No";
    }
    cout << "Yes";
  }

  
    
  
    return 0;
}
